import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pre-boot-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
